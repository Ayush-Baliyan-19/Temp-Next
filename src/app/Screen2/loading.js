import React from 'react'

const Loading = () => {
  return (
    <div className='bg-primary text-2xl text-secondary h-screen w-screen flex justify-center items-center'>Data is being loaded, please wait...</div>
  )
}

export default Loading